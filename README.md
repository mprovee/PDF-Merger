# PDF-Merger
PDF Merging App w/ GUI
The app relies on tkinter for the GUI and PyPDF2 for PDF merging
The program allows users to interact with a GUI and select PDF files stored on their computer, upload them, and merge them into a single, downloadable file.
